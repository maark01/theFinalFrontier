export namespace db {

    export interface Astronaut {
        id: string
        name: string
        status: string
    }

    export interface Agency {
        id: string
        name: string
    }
}